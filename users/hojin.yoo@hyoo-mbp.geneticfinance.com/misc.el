;; Prevent window split
;; (setq split-height-threshold 3000)
;; (setq split-width-threshold 2000)

;; to set PATH in remote server for TRAMP
;; (add-to-list 'tramp-remote-path 'tramp-own-remote-path)

(require 'setup-org)
